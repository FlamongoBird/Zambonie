Birds are fake.

# Commands that might be good to know

### Compile

`nim c main.nim`

Run Compiled code

`./main` :happy:

`main.exe` ... also it doesn't work on windows rn


### Compile and Run

`nim c -r main.nim`
